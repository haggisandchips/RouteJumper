<UserControl x:Class="RouteJumper.Views.TrackView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:vm="clr-namespace:RouteJumper.ViewModels"
             xmlns:converters="clr-namespace:RouteJumper.Converters"
             xmlns:behaviors="clr-namespace:RouteJumper.Behaviors"
             mc:Ignorable="d"
             d:DesignHeight="500" d:DesignWidth="700">

    <UserControl.Resources>
        <converters:StringToVisibilityConverter x:Key="StringToVisibilityConverter" />
    </UserControl.Resources>

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <TextBlock Text="Track" FontSize="16" FontWeight="Bold" Margin="0,0,0,8" />
            <TextBlock Text="Pick the running instance whose ship ED:FC Auto Pilot should track. No roles, no macros - Ship mode is passive tracking only; you plot and fly every jump yourself."
                       Opacity="0.6"
                       FontSize="11"
                       TextWrapping="Wrap" />
        </StackPanel>

        <Grid Grid.Row="1">
            <TextBlock Text="{Binding StatusText}"
                       Visibility="{Binding StatusText, Converter={StaticResource StringToVisibilityConverter}}"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       Opacity="0.6"
                       FontStyle="Italic" />

            <Grid>
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding Instances}" HorizontalContentAlignment="Stretch">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel Orientation="Vertical" />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate DataType="{x:Type vm:EliteInstanceViewModel}">
                            <materialDesign:Card Margin="0,0,0,12" Padding="16">
                                <StackPanel>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>

                                        <StackPanel Grid.Column="0">
                                            <TextBlock Text="{Binding CommanderName}"
                                                       FontSize="18"
                                                       FontWeight="Bold"
                                                       TextTrimming="CharacterEllipsis" />
                                            <TextBlock Text="{Binding ProcessId, StringFormat='PID {0}'}"
                                                       Opacity="0.6"
                                                       Margin="0,0,0,8" />
                                        </StackPanel>

                                        <Button Grid.Column="1"
                                                Content="Track"
                                                Padding="10,4"
                                                FontSize="11"
                                                VerticalAlignment="Top"
                                                Command="{Binding DataContext.ToggleTrackedCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                CommandParameter="{Binding}"
                                                ToolTip="Track/untrack this instance's ship">
                                            <Button.Style>
                                                <Style TargetType="Button" BasedOn="{StaticResource MaterialDesignOutlinedButton}">
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsTracked}" Value="True">
                                                            <Setter Property="Background" Value="{DynamicResource MaterialDesign.Brush.Secondary}" />
                                                            <Setter Property="Foreground" Value="{DynamicResource MaterialDesign.Brush.Secondary.Foreground}" />
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Button.Style>
                                        </Button>
                                    </Grid>

                                    <Grid Margin="0,4,0,0">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto" />
                                            <RowDefinition Height="Auto" />
                                        </Grid.RowDefinitions>

                                        <materialDesign:PackIcon Grid.Row="0" Grid.Column="0"
                                                                  Kind="MapMarkerOutline"
                                                                  VerticalAlignment="Center"
                                                                  Margin="0,0,8,0" />
                                        <TextBlock Grid.Row="0" Grid.Column="1"
                                                    Text="{Binding LocationDisplay}"
                                                    VerticalAlignment="Center"
                                                    TextTrimming="CharacterEllipsis" />

                                        <materialDesign:PackIcon Grid.Row="1" Grid.Column="0"
                                                                  Kind="FileDocumentOutline"
                                                                  VerticalAlignment="Center"
                                                                  Margin="0,6,8,0" />
                                        <TextBlock Grid.Row="1" Grid.Column="1"
                                                    Text="{Binding JournalFileName}"
                                                    VerticalAlignment="Center"
                                                    Margin="0,6,0,0"
                                                    TextTrimming="CharacterEllipsis"
                                                    Cursor="Hand"
                                                    ToolTip="Click to copy the journal filename to the clipboard"
                                                    behaviors:ClickCommandBehavior.Command="{Binding DataContext.CopyJournalFileNameCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                    behaviors:ClickCommandBehavior.CommandParameter="{Binding JournalFileName}" />
                                    </Grid>
                                </StackPanel>
                            </materialDesign:Card>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
                </ScrollViewer>
            </Grid>
        </Grid>

        <StackPanel Grid.Row="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,10,0,0">
            <Button Command="{Binding RefreshCommand}" Padding="8,4" AutomationProperties.Name="Refresh">
                <StackPanel Orientation="Horizontal">
                    <materialDesign:PackIcon Kind="Refresh" VerticalAlignment="Center" Margin="0,0,6,0" />
                    <TextBlock Text="Refresh" VerticalAlignment="Center" />
                </StackPanel>
            </Button>
        </StackPanel>
    </Grid>
</UserControl>
